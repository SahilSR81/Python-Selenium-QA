# Tuple is immutable

numbers = (10, 20, 30)

# numbers[0] = 100  # This will cause error

print(numbers)

# Output:
# (10, 20, 30)

# Explanation:
# Tuple does not allow modification after creation

